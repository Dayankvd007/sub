# AI Subtitle Translator — Development Roadmap v0.1

## Roadmap

| Phase | Task ID | Task | Description | Priority | Status | Dependencies | Deliverable | Exit Criteria | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Phase 0 — Caption Extraction Feasibility | P0-01 | Create GitHub repository | Create the single project repository and a minimal documentation skeleton. Do not add product implementation beyond the authorized phase. | High | Completed | — | Repository with an initial README and planned top-level areas | Repository is accessible, the default branch is set, and the scope is documented as a personal MVP. | Execution task; start only after roadmap approval. |
| Phase 0 — Caption Extraction Feasibility | P0-02 | Create minimal Manifest V3 extension | Set up a loadable TypeScript/Vite extension using Manifest V3 and plain HTML/TypeScript, with only the minimum permissions needed for the experiment. | High | Completed | P0-01 | Loadable unpacked extension build | The extension loads in the owner's Chrome without manifest, permission, or console errors. | No React; no product UI beyond experiment controls. |
| Phase 0 — Caption Extraction Feasibility | P0-03 | Detect YouTube watch pages | Recognize supported YouTube watch-page URLs and ignore non-watch pages. | High | Completed | P0-02 | Watch-page detection result | Detection is correct on direct loads, reloads, and navigation from another YouTube page. | Browser lifecycle task. |
| Phase 0 — Caption Extraction Feasibility | P0-04 | Detect current video ID | Read the active video ID and update it when YouTube changes videos through single-page navigation. | High | Completed | P0-03 | Stable video identity event | The current video ID is correct after direct load and repeated in-app navigation, with stale state cleared. | Needed for job identity and cache keys later. |
| Phase 0 — Caption Extraction Feasibility | P0-05 | Experiment with main-world script injection | Test a narrowly scoped injected script that can access page-level YouTube data and communicate a sanitized result back to the extension. | Critical | Completed | P0-02 | Documented injection proof of concept | The injected script runs repeatably, returns only expected data, and does not expose uncontrolled page messages. | Experiment; exact extraction method is not yet confirmed. |
| Phase 0 — Caption Extraction Feasibility | P0-06 | Test caption-track discovery | Identify available English caption tracks in the active browser session and distinguish manual from auto-generated tracks where possible. | Critical | Completed | P0-05 | Caption-track discovery record | At least one usable English track is found on each selected manual and auto-caption test video, or a documented fallback is required. | Highest-risk technical assumption. |
| Phase 0 — Caption Extraction Feasibility | P0-07 | Capture timed-text data | Capture the full ordered timed-text payload for a selected English track from the browser session; the backend must not scrape YouTube. | Critical | Completed | P0-06 | Raw timed-text captures | Text and timing cover the tested video without unexplained missing ranges or reordered items. | Record Chrome and YouTube conditions for reproducibility. |
| Phase 0 — Caption Extraction Feasibility | P0-08 | Normalize output into cue format | Convert captured data into ordered cues containing cue_index, start_ms, end_ms, and english_text; save sanitized fixtures. | Critical | Completed | P0-07 | Normalized caption fixture files | Indexes are unique and ordered; timestamps are valid; required fields are non-empty; the fixture can be reused offline. | Canonical Phase 0 handoff format. |
| Phase 0 — Caption Extraction Feasibility | P0-09 | Test manually captioned video | Run the complete capture and normalization path on one manually captioned English YouTube video across repeat attempts. | Critical | Completed | P0-08 | Manual-caption fixture and test notes | Repeated runs produce complete, usable cues with stable ordering and timing in the owner's Chrome. | Required exit-gate evidence. |
| Phase 0 — Caption Extraction Feasibility | P0-10 | Test auto-captioned video | Run the same path on one auto-captioned English YouTube video and record rolling-caption or timing behavior. | Critical | Completed | P0-08 | Auto-caption fixture and test notes | Repeated runs produce complete, usable cues; known rolling behavior is documented for Phase 1 cleaning. | Required exit-gate evidence. |
| Phase 0 — Caption Extraction Feasibility | P0-11 | Pass Phase 0 exit gate | Review both fixtures, select the primary extraction method, record any narrow fallback, and document known failure cases. | Critical | Completed | P0-09, P0-10 | Phase 0 experiment record and approved fixture set | Caption extraction is reliable enough in the owner's Chrome; otherwise the project stops here and tests a narrower alternative. | Gate: do not begin Phase 1 until approved. |
| Phase 1 — Translation Engine CLI | P1-01 | Create CLI translator | Create a standalone command-line workflow that accepts a caption file and runs the translation pipeline without Chrome or FastAPI. | High | Completed | P0-11 | Runnable CLI entry point | A fixture can be submitted and produces a clearly reported success or bounded failure. | Keep the CLI as the fastest translation test harness. |
| Phase 1 — Translation Engine CLI | P1-02 | Support JSON3, VTT, and SRT input | Parse the three expected caption formats into the same internal cue contract while preserving timing and source order. | High | Completed | P1-01, P0-08 | Input adapters and normalized cue output | Representative JSON3, VTT, and SRT samples produce equivalent valid cue objects. | No audio or video processing. |
| Phase 1 — Translation Engine CLI | P1-03 | Build caption cleaning | Normalize whitespace, remove irrelevant markers when safe, and repair malformed short items without unnecessarily changing timestamps. | High | Completed | P1-02 | Deterministic caption-cleaning stage | Cleaning is repeatable, preserves cue coverage, and records uncertain cases rather than silently deleting text. | Source text remains auditable. |
| Phase 1 — Translation Engine CLI | P1-04 | Remove rolling duplicates | Detect high-confidence repeated prefixes or overlaps in adjacent auto-caption cues and retain only newly spoken text. | Critical | Completed | P1-03 | Rolling-duplicate cleanup with fixtures | Known duplicate patterns are removed without losing possible speech; uncertain overlaps are preserved and flagged. | Experiment against the Phase 0 auto-caption fixture. |
| Phase 1 — Translation Engine CLI | P1-05 | Create context windows | Group ordered cues into translation windows with enough surrounding context while keeping expected cue indexes explicit. | Critical | Completed | P1-04 | Window builder and comparison fixtures | Every source cue belongs to exactly one target window and window boundaries remain auditable. | Initial experiment: roughly 40–70 cues or 2–3 minutes; not a fixed decision. |
| Phase 1 — Translation Engine CLI | P1-06 | Test AI translation models | Compare candidate models on the same manual and auto-caption samples for Persian quality, cost, latency, and structured-output reliability. | Critical | Completed | P1-05 | Model comparison record and provisional selection | One candidate meets the quality bar with acceptable cost, latency, and malformed-output rate; otherwise revise the test. | An `OpenRouterProvider` was added (`OPENROUTER_API_KEY`/`OPENROUTER_MODEL` from the local environment only) and validated on a real YouTube JSON3 capture (482 cues, 478 speech) using `google/gemini-3.1-flash-lite`: 478/478 translated, 0 failed, 0 retries, 0 splits, 0 validation errors, ~$0.0019 total. Owner reviewed the resulting Persian SRT on the actual video and accepted quality. No second model was needed. |
| Phase 1 — Translation Engine CLI | P1-07 | Design translation prompt | Create a versioned prompt that asks for natural Persian, uses context, preserves meaning, and returns the strict cue-index contract. | Critical | Completed | P1-05 | Versioned translation prompt | The prompt produces readable Persian while preserving every requested index and avoiding commentary outside the schema. | Prompt version must participate in cache identity later. |
| Phase 1 — Translation Engine CLI | P1-08 | Add structured JSON output validation | Parse the response and reject missing, duplicated, unexpected, empty, or malformed cue entries before they reach output. | Critical | Completed | P1-07 | Validated structured translation result | Every expected cue index appears exactly once and invalid output fails visibly. | Never accept partially valid output silently. |
| Phase 1 — Translation Engine CLI | P1-09 | Add retry and split logic | Retry structural failures once with a targeted correction; on repeated failure, split the affected window through a bounded path. | Critical | Completed | P1-08 | Bounded recovery workflow | Malformed windows either recover or end in a clear failure without loops, uncontrolled calls, or loss of completed work. | Record retry and split counts for model evaluation. |
| Phase 1 — Translation Engine CLI | P1-10 | Generate Persian SRT output | Convert the complete validated translated cue set into a reviewable Persian SRT while preserving usable timing. | High | Completed | P1-09 | Complete Persian SRT file | The SRT opens in a standard player, covers all expected cues, and has valid timestamps and sequence numbering. | SRT is the Phase 1 review artifact. |
| Phase 1 — Translation Engine CLI | P1-11 | Pass translation quality exit gate | Translate a complete representative educational video and review naturalness, fidelity, terminology, readability, cost, latency, and structural integrity. | Critical | Completed | P1-06, P1-10 | Approved full-video translation and experiment decisions | The owner considers the Persian comfortable for a complete educational video and every expected cue is present exactly once. | Gate passed 2026-07-25. Real YouTube JSON3 capture (business-related video): 482 total cues, 478 speech / 4 non-speech, 10 windows, 478/478 translated, 0 failed, 0 retries, 0 splits, 0 validation errors; ~1,132 prompt + 1,080 completion tokens, ~$0.0019 cost. Owner reviewed the generated Persian SRT on the actual video and accepted quality for the educational/business use case. Model: `google/gemini-3.1-flash-lite` via `OpenRouterProvider`. |
| Phase 2a — API Foundation | P2a-01 | Create FastAPI application | Expose the validated pipeline through a small Python/FastAPI service with GET /health, localhost-only binding, and an explicit origin allowlist. | High | Completed | P1-11 | Running local API and health response | The service starts locally, reports health, rejects unintended origins, and does not expose provider credentials. | Delivered as `subtitle-api` (`api/app.py`, `api/routes.py`, `api/runner.py`). Binds 127.0.0.1; CORS allowlist from `ALLOWED_ORIGINS`, never a wildcard. `GET /health` reports provider/model and credential *presence* only. |
| Phase 2a — API Foundation | P2a-02 | Add translation service layer | Insert a framework-free service between the API and the Phase 1 engine so the engine stays reusable and independently testable. | High | Completed | P2a-01 | `TranslationService` with no web-framework imports | The API calls the engine only through the service; the Phase 1 pipeline is unmodified. | `service.py`. Flow is API → TranslationService → `pipeline.translate_cues` → provider. Phase 1 engine files are byte-identical. |
| Phase 2a — API Foundation | P2a-03 | Implement POST /translate | Accept normalized cues from the extension, run the full pipeline synchronously, and return validated Persian cues, stats, and optional SRT. | Critical | Completed | P2a-02 | Extension-ready translation endpoint | Valid requests translate every speech cue; invalid requests receive typed errors; no credential or provider detail leaks. | Synchronous for Phase 2a. Client `cue_index` is preserved exactly and echoed back (the extension owns cue identity). `status` is job-shaped (`completed`/`partial`) so Phase 2b-2 can add async jobs without breaking the contract. |
| Phase 2a — API Foundation | P2a-04 | Add contracts, configuration, and limits | Define Pydantic request/response schemas, environment-based settings, request caps, and sanitized error mapping. | High | Completed | P2a-03 | Typed API contract and bounded requests | Malformed payloads fail at the boundary before any provider call; caps bound worst-case cost. | `api/schemas.py`, `api/settings.py`, `api/deps.py`, `.env.example`. `MAX_CUES=5000`, `MAX_BODY_BYTES=5 MB`. Errors are `{error_code, message}`: 400/413/422/502/500. Credentials stay environment-only, never in settings or responses. |
| Phase 2a — API Foundation | P2a-05 | Pass API foundation gate | Test both endpoints and the service layer offline, and confirm the Phase 1 suite is unaffected. | Critical | Completed | P2a-04 | Phase 2a test evidence and a working local service | Existing tests still pass, new endpoint/service tests pass, and the service runs for real on localhost. | 66 tests pass with the `[api]` extra (39 pre-existing + 27 new); 47 pass / 2 skip without it, so the engine keeps zero required dependencies. Real uvicorn smoke test returned valid Persian SRT and usage stats. Gate: Phase 2b-1 may begin; extension integration still waits on the Phase 2b-2 exit gate. |
| Phase 2b-1 — Persistence and Cache | P2b1-01 | Add SQLite database | Create the minimal videos and cues persistence model, including status, model, prompt_version, caption_fingerprint, cue timing, texts, and timestamps. | High | Completed | P2a-05 | SQLite schema and bootstrap path | Validated cues persist across process restarts without enterprise-style extra tables. | `db.py` + `repository.py`. Schema is exactly architecture §7 (two tables, no extras); the `status` column already accommodates Phase 2b-2's `queued`/`processing` without migration. Database lives in a per-user data directory via `SUBTITLE_DB_PATH`, never inside the repository. Each operation opens a short-lived connection, so the FastAPI threadpool is safe today and the 2b-2 worker will be too. |
| Phase 2b-1 — Persistence and Cache | P2b1-02 | Compute caption fingerprints | Derive a stable fingerprint from received cues so cache identity does not depend on a client-supplied hash. | High | Completed | P2b1-01 | Deterministic caption fingerprint | Identical captions produce the same fingerprint; any real change to text, timing, or ordering produces a different one. | `fingerprint.py`: sha256 over loader-normalized cues. Computed by the backend, so the client contract is unchanged. Cosmetic whitespace differences do not miss the cache. |
| Phase 2b-1 — Persistence and Cache | P2b1-03 | Implement translation cache | Build cache identity from video_id, caption fingerprint, model, and prompt_version; reuse only an exact completed match. | Critical | Completed | P2b1-02 | Verified cache-hit path | A repeated identical request returns stored results without a model call; changed source, model, or prompt creates a distinct identity. | Cache sits around `translate_cues()` in `service.py`. Lookup runs before the provider is constructed, so a cache hit needs no API key. Only a fully completed record is served — a partial result is retranslated in place, never presented as finished. |
| Phase 2b-1 — Persistence and Cache | P2b1-04 | Add cache-hit behaviour to POST /translate | Return stored translations with a `cache_hit` flag, leaving the existing endpoint contract otherwise unchanged. | High | Completed | P2b1-03 | Extension-visible cache reuse | A repeat request returns the same cues and SRT with zero provider calls. | `cache_hit` added to the response; `POST /translate` is otherwise unchanged. Stats on a hit report `provider_calls=0`, describing this request rather than replaying the original run. |
| Phase 2b-1 — Persistence and Cache | P2b1-05 | Correct usage aggregation | Report token and cost totals across every translation window instead of the most recent call. | Medium | Completed | P2a-05 | Per-request usage totals | Reported usage equals the sum across all windows, not the final window's figures. | `OpenRouterProvider` overwrote `last_usage` per call, so a run total was reported as one window's numbers. `usage_totals` now accumulates; the service and CLI surface it. See the documentation-correction note below — historical Phase 1 figures are affected and are NOT rewritten here. |
| Phase 2b-1 — Persistence and Cache | P2b1-06 | Pass persistence and cache gate | Verify cache hits, invalidation, restart safety, and partial handling offline. | Critical | Completed | P2b1-04, P2b1-05 | Phase 2b-1 test evidence | A repeat is a cache hit with zero provider calls; each identity field invalidates independently; completed cues survive restart; partial results are never served as complete. | 96 tests pass with the `[api]` extra (66 pre-existing + 30 new); 73 pass / 3 skip without it, so the engine keeps zero required dependencies. Cache hits are asserted by provider call count. Verified against a real uvicorn process: request 1 `cache_hit=false, provider_calls=1`; request 2 `cache_hit=true, provider_calls=0`. |
| Phase 2b-2 — Jobs and Progressive Delivery | P2-03 | Implement translation jobs | Create in-process job coordination that accepts ordered cues, invokes the validated pipeline, tracks progress, and persists validated work. | Critical | Completed | P2a-03, P2b1-01 | Recoverable translation job lifecycle | Jobs move through clear states, persist validated progress before exposure, and end as completed or terminally failed. | `jobs.py`: `JobService` / `JobRunner` / `JobWorker`, all framework-free. Mechanism selected: **one background thread**, one job at a time, sequential windows — the provider is blocking stdlib `urllib` and `sqlite3` is blocking, so asyncio would have meant rewriting validated Phase 1 code and adding dependencies for parallelism a single user does not need. A `videos` record *is* the job record (`videos.id` = `job_id`), so the UNIQUE cache identity is also the idempotency key. `partial` is terminal (finished with failed cues); mid-run progress is `processing` plus counters. |
| Phase 2b-2 — Jobs and Progressive Delivery | P2-05 | Implement API endpoints | Implement POST /jobs, GET /jobs/{id}, and the response contracts; keep GET /jobs/{id}/srt compatible for future/on-demand export. | High | Completed | P2-03 | Documented and tested local API contract | Valid requests create or reuse a job, status reads return only validated cues, and invalid requests receive clear errors. | `POST /jobs` (201 created; 200 for reuse / resume / cache hit) and `GET /jobs/{id}`. `job_id` is `videos.id`. Errors: 400 `invalid_cues`, 404 `job_not_found`, 409 `job_in_progress`, 413 `too_many_cues`, 503 `jobs_unavailable`. A separate `GET /jobs/{id}/srt` was not built — `?include_srt=true` renders the full file from stored cues, leaving that path free. `POST /translate` is unchanged apart from the 409 guard. |
| Phase 2b-2 — Jobs and Progressive Delivery | P2-06 | Add recovery handling | Preserve completed cues, classify recoverable versus terminal failures, and resume safely after provider, network, or backend interruption. | Critical | Completed | P2b1-01, P2-03 | Restart and failure recovery behavior | A restart retains completed cues; transient failures do not restart from zero; terminal failures are visible and bounded. | Every window is committed to SQLite as it finishes, so a crash loses at most the window in flight. SQLite is the queue of record; the in-memory queue is only a doorbell. Startup sweeps `queued`/`processing`, increments `attempt_count`, and re-queues — or fails past the limit so a crash loop cannot burn budget. Resume re-derives the deterministic windows and translates only cues with no committed translation. Provider transport failure: 2 retries (2 s, 8 s) from the last commit, then `failed` with translated cues kept. |
| Phase 2b-2 — Jobs and Progressive Delivery | P2-07 | Add polling support | Return job progress and newly validated cues through simple polling, with a cursor or equivalent duplicate-safe incremental contract. | High | Completed | P2-03, P2-05 | Incremental polling response | Repeated polling is idempotent, does not duplicate cues, and clearly distinguishes partial, complete, retrying, and failed states. | Cursor is `after_cue_index`, with `next_cursor` echoed back; no per-cue sequence column was needed. Policy: **any validated range** plus client-side `cue_index` dedup plus one final cursor-less reconciliation read once `done` is true — safe against a hole left by a failed window, which a contiguous-prefix-only policy would stall on forever. Only cues with non-empty stored Persian are ever returned. |
| Phase 2b-2 — Jobs and Progressive Delivery | P2-08 | Pass backend exit gate | Run integration tests covering a full fixture job, cache hit, partial job, invalid request, provider failure, and backend restart. | Critical | Completed | P2-05, P2-06, P2-07 | Backend test evidence and approved API | A full fixture completes, restart preserves progress, an exact repeat is a cache hit, and invalid model output never reaches the client. | 153 tests pass with the `[api]` extra (96 pre-existing + 57 new); 112 pass / 4 skip without it. Real uvicorn run with the real worker thread: 400-cue job returned a `job_id` in 0.04 s, delivered cues while still `processing`, completed with all 400 present exactly once across cursor polling with zero duplicates; repeat POST was a cache hit with inline cues and no provider call. A 5000-cue job was **`SIGKILL`ed mid-translation** (1700 cues committed); after restart 3850 committed cues had survived and the sweep resumed it to completion with all 5000 present exactly once. Gate passed 2026-07-25: Phase 3 may begin. |
| Phase 3 — Chrome Extension | P3-01 | Add subtitle button | Add a minimal Persian subtitle control and clear states for ready, no captions, backend unavailable, translating, completed, retrying, and failed. | High | Not Started | P2-08 | Minimal in-page subtitle control | The control appears only on supported watch pages and its state matches the active video and backend condition. | Plain HTML/TypeScript; no React. |
| Phase 3 — Chrome Extension | P3-02 | Connect extension to backend | Use the local API client for health checks and origin-safe communication, with cleanup when the video changes. | High | Not Started | P3-01, P2a-01 | Working extension-to-backend connection | Health state is accurate and no request or response leaks across video navigation. | Provider credentials remain backend-only. |
| Phase 3 — Chrome Extension | P3-03 | Send caption data | Submit video metadata, caption fingerprint, and ordered normalized cues to POST /jobs using the validated Phase 0 extractor. | Critical | Not Started | P3-02, P0-11 | Created or reused translation job | The backend accepts the correct active-video payload and reports a job ID or exact cache hit without duplicated submission. | Use browser-side caption capture; do not add backend scraping. |
| Phase 3 — Chrome Extension | P3-04 | Receive translated cues | Poll GET /jobs/{id}, validate client-facing response shape, and insert each newly validated cue at most once. | Critical | Not Started | P3-03, P2-07 | Duplicate-safe translated cue stream | Partial and complete results arrive in order and repeated polls never create duplicate rendered cues. | Complete-result path may be proven first if integration risk is lower. |
| Phase 3 — Chrome Extension | P3-05 | Create TextTrack and VTTCue | Create a dedicated subtitle track and map validated cue timing to VTTCue objects driven by the video media clock. | Critical | Not Started | P3-04 | Synchronized Persian TextTrack | Cue activation remains correct during basic pause, resume, seeking, speed changes, and fullscreen tests. | Current decision; validate with real playback evidence. |
| Phase 3 — Chrome Extension | P3-06 | Render Persian RTL overlay | Display the active cue through a safe custom right-to-left overlay while TextTrack remains the timing source. | High | Not Started | P3-05 | Readable Persian overlay inside YouTube | Persian is correctly ordered, centered, legible, and does not expose raw HTML or stale text. | Minimal styling; readability over decoration. |
| Phase 3 — Chrome Extension | P3-07 | Pass first workflow exit gate | Run the complete capture-to-render workflow on the manual-caption and auto-caption test videos and exercise basic playback and navigation. | Critical | Not Started | P3-06 | First end-to-end YouTube subtitle workflow evidence | Both test videos display validated Persian; pause, resume, seek, speed, fullscreen, and video navigation show no stale or mistimed text. | Gate: reliability work starts only after the basic workflow works. |
| Phase 4 — Reliability and Polish | P4-01 | Improve progressive translation | Append newly validated cues while translation continues and preserve the longest safe usable result without duplicates or false completion. | High | Not Started | P3-07, P2-07 | Reliable progressive subtitle delivery | Early sections appear while later work continues; repeated polling and retries do not duplicate or reorder cues. | Choose any-range versus contiguous-range policy from evidence. |
| Phase 4 — Reliability and Polish | P4-02 | Handle SPA navigation | Harden cleanup and reinitialization across repeated YouTube single-page navigation and history changes. | High | Not Started | P3-07 | Navigation-safe lifecycle | Old tracks, overlays, polling, and job state are removed before the new video is initialized. | Regression test repeated back-and-forth navigation. |
| Phase 4 — Reliability and Polish | P4-03 | Handle fullscreen and resizing | Keep the overlay positioned and readable across fullscreen entry/exit, theater mode, and player resizing. | Medium | Not Started | P3-07 | Responsive overlay behavior | The overlay remains visible, inside the player, and readable at the owner's common player sizes. | Test actual Google Chrome rendering. |
| Phase 4 — Reliability and Polish | P4-04 | Handle seeking and playback speed | Harden subtitle activation across repeated seeks and 0.75×, 1×, 1.5×, and 2× playback. | High | Not Started | P3-07 | Playback synchronization regression tests | The visible Persian cue matches the media time immediately after seeks and remains synchronized at tested speeds. | Test both manual and auto-caption fixtures. |
| Phase 4 — Reliability and Polish | P4-05 | Handle ads | Detect an evidence-backed ad state, hide or suspend the overlay as needed, and restore it without stale cues. | Medium | Not Started | P3-07 | Ad transition behavior | At least one available ad transition completes without showing unrelated Persian text or corrupting the active job. | Exact ad-state signal remains an experiment. |
| Phase 4 — Reliability and Polish | P4-06 | Improve errors and recovery | Make backend-unavailable, no-captions, retrying, partial, failed, and recovered states understandable and safely actionable. | High | Not Started | P4-01 | User-visible recovery states | Routine temporary failures recover or provide a clear retry path without losing completed cues or requiring a full restart. | Differentiate automatic retry from user action. |
| Phase 4 — Reliability and Polish | P4-07 | Improve subtitle readability | Tune font-size defaults, RTL layout, line length, and evidence-based condensation or segmentation; persist only the minimal useful preference. | Medium | Not Started | P3-07 | Readable overlay settings and segmentation decision | Long and fragmented cues remain readable without materially changing meaning, breaking indexes, or adding unnecessary settings. | Use the subtitle segmentation experiment; no style feature expansion. |
| Phase 4 — Reliability and Polish | P4-08 | Pass Personal MVP exit gate | Run the PRD success criteria on a representative set of educational videos and complete normal viewing sessions without routine manual recovery. | Critical | Not Started | P4-01, P4-02, P4-03, P4-04, P4-05, P4-06, P4-07 | Approved personal MVP and final known-issues list | Extraction, translation quality, caching, recovery, synchronization, progressive delivery, and readability are acceptable for regular personal use. | Final gate; no SaaS, cloud, accounts, or unnecessary framework work. |

## Experiments

| Experiment | Phase | Hypothesis | Test Method | Result | Decision | Date |
| --- | --- | --- | --- | --- | --- | --- |
| Caption extraction method | Phase 0 — Caption Extraction Feasibility | Browser-side capture, using a narrowly scoped main-world injection path, can produce complete ordered cues for manual and auto captions without backend scraping. | Use one fixed manual-caption and one fixed auto-caption video. Repeat direct load, reload, and SPA navigation. Compare cue count, indexes, timing, text coverage, missing ranges, and repeatability; record Chrome/YouTube conditions and test a narrow fallback only if needed. | Pass (owner-validated 2026-07-22). Extension built and unit-tested; owner ran the real two-video capture in their own Chrome and reported success. Raw fixtures + per-video metrics still to be committed by owner (see docs/PHASE0_EXPERIMENT_NOTES.md). | Selected primary method: main-world `chrome.scripting.executeScript({world:'MAIN'})` reading the player response and fetching the selected English track as `&fmt=json3`, normalized by `normalize.ts`. No fallback needed yet. | 7/22/2026 |
| AI model comparison | Phase 1 — Translation Engine CLI | At least one affordable candidate can produce natural Persian and satisfy the strict indexed JSON contract with acceptable latency and reliability. | Translate identical manual and auto-caption windows with each candidate. Score naturalness, fidelity, terminology, segmentation, cost, latency, malformed-output rate, retry rate, and a complete educational-video sample. | Passed — an `OpenRouterProvider` was added on the existing `TranslationProvider` contract, reading `OPENROUTER_API_KEY`/`OPENROUTER_MODEL` from the local environment only. Validated on a real YouTube JSON3 capture from a business-related video (482 cues, 478 speech, 10 windows) with `google/gemini-3.1-flash-lite`: 478/478 translated, 0 failed/retries/splits/validation errors, ~$0.0019 total cost. The owner reviewed the resulting Persian SRT on the actual video and accepted the quality. | Selected: OpenRouter / `google/gemini-3.1-flash-lite`. Revisitable later if a future need calls for a different model. | 7/25/2026 |
| Translation window size | Phase 1 — Translation Engine CLI | An initial range near 40–70 cues or 2–3 minutes provides enough context without causing unacceptable latency or structured-output failures. | Test several window sizes on the same fixtures. Measure Persian quality, missing/duplicate indexes, malformed responses, retry/split frequency, latency, and cost; compare manual versus rolling auto captions. | Default implemented: target 50 / max 70 cues, 2 context cues per side (CLI-tunable). Empirical size/latency/cost sweep still pending the owner's live run. | Provisional defaults set; not yet locked pending evidence. | 7/22/2026 |
| Subtitle segmentation | Phase 1 — Translation Engine CLI | One Persian output per source cue, with context-aware wording and limited condensation, is readable enough for MVP timing; only observed failures justify redistribution. | Review long, fragmented, and rapid cue sequences in generated SRT and in-player playback. Compare readability, semantic fidelity, line length, timing, and index integrity before considering limited redistribution. | Pending | Pending — retain one-to-one cue mapping unless evidence shows a narrow segmentation change is necessary. |  |

## Decisions

| Decision | Date | Reason | Status | Related Phase |
| --- | --- | --- | --- | --- |
| Local backend | 7/22/2026 | Keeps credentials, persistence, and processing on the owner's computer and avoids cloud infrastructure for a one-user personal tool. | Current Decision | Phase 2a / 2b-1 / 2b-2 |
| FastAPI | 7/22/2026 | Provides a small typed Python HTTP layer around the validated translation engine and a simple local health check. | Implemented in Phase 2a | Phase 2a — API Foundation |
| SQLite | 7/22/2026 | Provides sufficient durable storage for one user, exact cache reuse, progressive results, and restart recovery without extra infrastructure. | Implemented in Phase 2b-1 | Phase 2b-1 — Persistence and Cache |
| Split Phase 2 into 2a (API foundation) and 2b (persistence and jobs) | 7/25/2026 | Delivers a working extension-callable API first and defers persistence, caching, jobs, recovery, and polling, so the HTTP contract can be proven before storage design is committed. | Current Decision | Phase 2a / Phase 2b |
| Split Phase 2b into 2b-1 (persistence and cache) and 2b-2 (jobs and progressive delivery) | 7/25/2026 | The cache is the highest-value, lowest-risk half: it removes the repeat-video wait with no threads, no new endpoints, and no lifecycle states. Jobs and polling introduce the project's first concurrency and are only justified once real end-to-end latency is measured. | Current Decision | Phase 2b-1 / Phase 2b-2 |
| Backend computes the caption fingerprint | 7/25/2026 | Derived from the cues the backend already receives, so the client contract is unchanged and no client-supplied hash has to be trusted. Hashed over loader-normalized cues, so cosmetic whitespace does not miss the cache while real caption changes do. | Current Decision | Phase 2b-1 — Persistence and Cache |
| SQLite database stored outside the repository | 7/25/2026 | `SUBTITLE_DB_PATH` defaults to a per-user data directory, so local translation data is never committed and never collides with a checkout. | Current Decision | Phase 2b-1 — Persistence and Cache |
| Cache hit requires no provider API call or credentials | 7/25/2026 | Lookup uses the configured model and runs before the provider is constructed, so a repeat request costs nothing and works without an API key. | Current Decision | Phase 2b-1 — Persistence and Cache |
| Usage reported per request, not per last window | 7/25/2026 | `OpenRouterProvider` overwrote `last_usage` on every call, so a multi-window run reported one window's tokens and cost as the run total. Totals are now accumulated across all windows. Historical Phase 1 figures are affected; see the documentation-correction note below. | Current Decision | Phase 2b-1 — Persistence and Cache |
| Phase 1 engine stays unchanged under the API | 7/25/2026 | The API is additive: a framework-free `TranslationService` wraps `pipeline.translate_cues`, so the validated engine keeps its Phase 1 evidence and stays usable from the CLI. Phase 1 engine files are byte-identical after Phase 2a. | Current Decision | Phase 2a — API Foundation |
| Synchronous `POST /translate` for Phase 2a | 7/25/2026 | One request translates the whole video, which is simple and adequate for a single local user. The response `status` field is job-shaped (`completed`/`partial`) so Phase 2b-2 can add async jobs and polling without breaking the extension contract. Cost: a long video occupies the connection for minutes. | Revisited in Phase 2b-2: kept unchanged alongside `POST /jobs`, plus a 409 guard | Phase 2a — API Foundation |
| Latency-measurement gate for Phase 2b-2 waived | 7/25/2026 | The architecture doc made measured end-to-end latency the entry condition for jobs. The owner waived it: the value of jobs is progressive delivery and reliable long-running translation, not latency reduction alone. | Owner Decision | Phase 2b-2 — Jobs and Progressive Delivery |
| Job layer beside `TranslationService`, not underneath it | 7/25/2026 | `POST /translate` keeps its Phase 2a/2b-1 contract and test evidence while `POST /jobs` adds progressive delivery. Both share one database and one cache identity, so work done by either path is reused by the other. | Current Decision | Phase 2b-2 — Jobs and Progressive Delivery |
| A `videos` record is the job record | 7/25/2026 | `videos.id` is the `job_id`, so the existing UNIQUE cache identity doubles as the job's idempotency key — a repeated create cannot start a second translation of the same thing. No jobs table, per architecture §7; four nullable columns were added instead. | Current Decision | Phase 2b-2 — Jobs and Progressive Delivery |
| Single background worker thread, one job at a time | 7/25/2026 | The provider uses blocking stdlib `urllib` and `sqlite3` is blocking, so an asyncio worker would have meant rewriting Phase-1-validated code and adding dependencies to buy parallelism a single user does not need. Concurrency of one also bounds provider cost and rate-limit exposure. No Redis, Celery, or broker. | Current Decision | Phase 2b-2 — Jobs and Progressive Delivery |
| Commit every translation window to SQLite | 7/25/2026 | One property delivers both goals: results become pollable while translation continues, and a crash loses at most the window in flight (~70 cues, well under a cent). | Current Decision | Phase 2b-2 — Jobs and Progressive Delivery |
| SQLite is the queue of record; the in-memory queue is only a doorbell | 7/25/2026 | The startup recovery sweep rediscovers every non-terminal job, so queue durability is a non-problem and no broker is justified. | Current Decision | Phase 2b-2 — Jobs and Progressive Delivery |
| `partial` is a terminal status, not a mid-run state | 7/25/2026 | Phase 2b-1 already stored `partial` to mean "finished with failed cues"; a second mid-run meaning would have been a bug source. Mid-run progress is carried by `processing` plus counters. Supersedes the lifecycle diagram in architecture §4. | Current Decision | Phase 2b-2 — Jobs and Progressive Delivery |
| Progressive delivery returns any validated range | 7/25/2026 | Cursor `after_cue_index` plus client-side `cue_index` dedup (already required by P3-04) plus one final cursor-less reconciliation read. Safe against a hole left by a permanently failed window, which a contiguous-prefix-only policy would stall on forever. Resolves the architecture §5 open question. | Current Decision | Phase 2b-2 / Phase 3 |
| Phase 1 engine gains only an optional `on_window_done` callback | 7/25/2026 | Per-window commits need a hook into the pipeline's window loop. The callback is inert by default, so the CLI and `POST /translate` behave exactly as before and every Phase 1 test passes unmodified. The alternatives — duplicating the clean/dedup/window orchestration in the job layer, or refactoring `translate_cues` into a generator — were both larger and riskier. This is the only change to the Phase 1 engine. | Owner-approved Decision | Phase 2b-2 — Jobs and Progressive Delivery |
| `POST /translate` returns 409 while a job holds the same identity | 7/25/2026 | `Repository.save()` rewrites a record's cues wholesale, so a synchronous translation would delete a running job's committed progress. A guard, not a contract change — no field added or removed. | Current Decision | Phase 2b-2 — Jobs and Progressive Delivery |
| No cancellation endpoint and no retention policy | 7/25/2026 | A job whose viewer navigated away simply finishes and populates the cache, which is the useful outcome for a personal tool. The database grows by roughly one row per translated video. | Out of Scope | Phase 2b-2 — Jobs and Progressive Delivery |
| Extension owns `cue_index` | 7/25/2026 | The backend preserves and echoes client-supplied cue indexes exactly and never renumbers, so the extension can map translated cues back to its own captured cues. Non-speech and failed cues are omitted and reported rather than faked. | Current Decision | Phase 2a / Phase 3 |
| FastAPI as an optional `[api]` extra | 7/25/2026 | Keeps the Phase 1 engine and its tests free of required third-party dependencies; the engine suite still runs when the extra is absent (47 pass / 2 skip). | Current Decision | Phase 2a — API Foundation |
| TypeScript extension | 7/22/2026 | Improves message-contract and lifecycle safety in a changing browser integration while remaining lightweight with Vite and plain HTML. | Current Decision | Phase 0 / Phase 3 |
| TextTrack synchronization | 7/22/2026 | Uses the media clock for cue activation while a custom RTL overlay controls Persian rendering; real playback behavior must still be validated. | Current Decision | Phase 3 — Chrome Extension |
| Polling instead of WebSockets | 7/22/2026 | Simple polling is adequate for one local client and incremental results, with less lifecycle and infrastructure complexity. | Current Decision | Phase 2b-2 / Phase 3 |
| No React initially | 7/22/2026 | The MVP interface is too small to justify framework complexity; plain HTML and TypeScript are sufficient. | Current Decision | Phase 0 / Phase 3 |
| Translation provider/model: Anthropic `claude-sonnet-5` | 7/22/2026 | Strong Persian/multilingual quality and reliable strict-JSON output at moderate cost; provider abstraction keeps it a one-line change. Provisional pending owner's live comparison (P1-06). | Superseded by OpenRouter trial below | Phase 1 — Translation Engine CLI |
| OpenRouter provider added (`OpenRouterProvider`) | 7/25/2026 | Adds a second `TranslationProvider` implementation (OpenAI-compatible chat-completions API) so a wider set of models, including `google/gemini-3.1-flash-lite`, can be trialed without touching the pipeline. Credentials read only from the local `OPENROUTER_API_KEY`/`OPENROUTER_MODEL` environment; PR merged. | Current Decision | Phase 1 — Translation Engine CLI |
| Selected model: `google/gemini-3.1-flash-lite` via OpenRouter | 7/25/2026 | Passed the P1-11 exit gate on a real YouTube JSON3 capture (482 cues, 478 speech): 478/478 translated, 0 failed/retries/splits/validation errors, ~$0.0019 total cost. The owner reviewed the generated Persian SRT on the actual video and accepted quality for the educational/business use case. Model choice can still be revisited later if evidence warrants. | Current Decision | Phase 1 — Translation Engine CLI |
| Prompt version v1 | 7/22/2026 | Versioned prompt loaded from a Markdown spec; participates in cache identity (implemented in Phase 2b-1), so changes bump the version rather than editing in place. | Current Decision | Phase 1 — Translation Engine CLI |

## Milestones

| Milestone | Target | Status | Evidence Required |
| --- | --- | --- | --- |
| Caption extraction validated | Phase 0 exit gate | Completed (owner-validated 2026-07-22) | Extension built, `normalize.ts` unit-tested against synthetic manual + auto json3 fixtures; owner-reported successful real two-video capture in their Chrome; primary extraction method documented. Follow-up: owner to commit raw `real-*.json` fixtures + per-video metrics into `extension/tests/fixtures/`. |
| Translation quality validated | Phase 1 exit gate | Completed (2026-07-25) | Engine implemented in `backend/` and tested (39 pytest tests, mock provider): loaders, cleaning, rolling dedup, windowing, strict-JSON validation, bounded retry/split, RTL SRT. `OpenRouterProvider` added and merged; validated on a real YouTube JSON3 capture (482 cues, 478 speech, 10 windows) with `google/gemini-3.1-flash-lite` — 478/478 translated, 0 failed/retries/splits/validation errors, ~$0.0019 total cost. Owner manually reviewed the generated Persian SRT on the actual video and accepted quality; final model-selection decision recorded. See docs/PHASE1_TRANSLATION_ENGINE_NOTES.md. |
| Local API foundation working | Phase 2a gate | Completed (2026-07-25) | `subtitle-api` FastAPI service over the unchanged Phase 1 engine: `GET /health`, `POST /translate`, `TranslationService` layer, Pydantic contracts, env-based config, CORS allowlist, request caps, sanitized errors, 127.0.0.1 binding. 66 tests pass with the `[api]` extra (39 pre-existing + 27 new); 47 pass / 2 skip without it. Real uvicorn smoke test returned valid Persian SRT and usage stats. |
| Persistence and cache working | Phase 2b-1 gate | Completed (2026-07-25) | SQLite persistence, repository layer, backend-computed caption fingerprint, translation cache, `cache_hit` on `POST /translate`, restart-safe storage, and the usage-aggregation correction. Cache hit returns the stored translation with **zero provider calls**; invalidation verified independently for `video_id`, caption fingerprint, model, and `prompt_version`; partial translations are never served as completed; usage totals aggregate across all windows. 96 tests pass with the `[api]` extra (66 pre-existing + 30 new); 73 pass / 3 skip without it. |
| Backend working | Phase 2b-2 exit gate | Completed (2026-07-25) | Jobs, progressive delivery, polling, and restart recovery over the Phase 2b-1 cache. 153 tests pass with the `[api]` extra (96 pre-existing + 57 new); 112 pass / 4 skip without it. Real uvicorn evidence: `job_id` returned in 0.04 s, cues delivered mid-`processing`, duplicate-free cursor polling, cache hit with zero provider calls, and a `SIGKILL` mid-translation of a 5000-cue job that resumed after restart to all 5000 cues exactly once without re-translating committed work. |
| First YouTube subtitle workflow working | Phase 3 exit gate | Not Started | Manual-caption and auto-caption videos rendered in YouTube; validated TextTrack/VTTCue timing; RTL overlay; playback and navigation test evidence without stale text. |
| Personal MVP completed | Phase 4 exit gate | Not Started | Representative video test set passes extraction, quality, cache, recovery, progressive delivery, synchronization, ads when available, and readability; normal owner sessions need no routine manual recovery. |

## Known documentation corrections

| Item | Status | Detail |
| --- | --- | --- |
| Phase 1 token and cost figures are under-reported | **Correction PR required — not yet applied** | The Phase 1 evidence in this file (P1-06, P1-11, the AI-model-comparison experiment, the model-selection decision, and the Phase 1 milestone) and in `docs/PHASE1_TRANSLATION_ENGINE_NOTES.md` records ~1,132 prompt tokens, ~1,080 completion tokens, and ~$0.0019 as **run totals**. They are not: `OpenRouterProvider` overwrote `last_usage` on every call, so those figures describe a **single translation window**, not the ten-window run. The true totals are roughly ten times larger (~$0.019 for that video). The reporting defect is fixed in Phase 2b-1 (P2b1-05); the historical numbers are **deliberately left unchanged here** rather than silently rewritten, and a separate correction PR must restate them with the reason. This does not affect the Phase 1 quality verdict, which rested on the owner's manual review of the Persian output, nor any decision taken since — the corrected cost is still negligible. |
